{
  "name": "orvion",
  "private": true,
  "version": "0.1.0",
  "description": "Orvion — Agent Execution System. The definitive execution layer for autonomous agents.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev -w @orvion/api-gateway",
    "dev:mobile": "npm run dev -w @orvion/mobile-pwa",
    "dev:admin": "npm run dev -w @orvion/admin-dashboard",
    "build:api": "npm run build -w @orvion/api-gateway",
    "build:mobile": "npm run build -w @orvion/mobile-pwa",
    "build:admin": "npm run build -w @orvion/admin-dashboard",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
