name: Security Scan

on:
  push:
    branches: [main]

jobs:
  audit:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Node audit
        run: npm audit || true

      - name: Python audit
        run: |
          pip install pip-audit
          pip-audit || true
